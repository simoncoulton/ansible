server {
    listen 80 default_server;
    server_name localhost;
    root /var/www/public;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/uwsgi.{{ vagrant_app.package }}.sock;
    }
}
